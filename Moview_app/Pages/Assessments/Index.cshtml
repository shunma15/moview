@page
@model Moview_app.Pages.Assessments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Assessment[0].MovieID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Assessment[0].AccountID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Assessment[0].Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Assessment[0].Posting)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Assessment[0].Value)
            </th>
            <th>
                
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Assessment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MovieID)
            </td>
            <td>
                @Html.DisplayFor(modelitem => item.AccountID)
            </td>
            <td>
                @Html.DisplayFor(modelitem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Posting)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
